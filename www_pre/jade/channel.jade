ion-view(view-title='{{channel.name}}')
  ion-content.padding
    .list.card(ng-repeat="clip in clips")
      .item.item-divider {{clip.userData.email}}
      .item.item-image
        a(href='#/tab/channel-chat/{{clip._id}}')
          img(src="{{clip.img}}")
  ion-footer-bar
    button.button(ng-click="newClip()") 发表截图

